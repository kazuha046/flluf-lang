use System; // We dont need `use System;` in all modules who imports here cuz it imports here

pub use reassignment::*; // Import module
//pub use reassignment::Re; // Also can be
//pub use reassignment::{Re, Do}; // Also can be

//pub use reassignment; // If we wanna type all path in main.fll
// (use tests::reassignment::*) so we need call func as tests::Do();
// or
// (use tests::*) so we need call func as reassignment::Do();

pub use some::*;
